# 回测分析报告 — {TICKER}

**回测窗口**：{DATA_START} → {DATA_END}（共 {BARS} 个交易日）
**生成日期**：{RUN_DATE}
**随机种子**：{SEED} · **测试策略数量**：{N_STRATEGIES}

---

## 执行摘要

{用 2-3 个完整句子说清三件事：测试了什么、按风险调整收益谁排第一、关键结论。面向"不在场"的读者写——完整句子，不要日志缩写。}

## 策略对比

| 排名 | 策略 | 总收益率 | 年化收益 | 最大回撤 | Sharpe | 交易次数 | 胜率 |
|:----:|------|--------:|---------:|---------:|-------:|--------:|------:|
| 1    | {name} | {ret}% | {ann}% | {dd}% | {sharpe} | {trades} | {wr}% |
| 2    | ...    | ...    | ...    | ...   | ...      | ...      | ...   |

按 Sharpe（风险调整收益）降序排列。`Sharpe = N/A` 表示该策略产生的信号不足以计算有意义的比率。年化收益按实际数据窗口（非配置的回测期间）折算。

## 风险标记

{若任何策略带 flag，逐条列出：策略名加粗 + 自然语言解释这意味着什么、为什么读者要在意。技术 flag 到中文含义的映射：}

- `negative_sharpe` — 风险调整后收益为负。在本回测窗口内策略表现甚至跑不赢无风险基准。
- `sharpe_unavailable` — 收益曲线过于平坦或样本太少，Sharpe 无法定义。绩效数字置信度低。
- `high_drawdown` — 最大回撤超过 20%。需要资金能扛住峰谷的下行。
- `no_trades` — 策略全程未开仓。入场参数对该标的或窗口而言可能过于严苛。
- `too_few_trades` — 闭合交易少于 5 次。样本量不足以支撑结论。

{若所有策略均无 flag，直接写："本次回测未触发任何关键风险标记。"}

## 部署建议

**推荐部署：{STRATEGY_NAME}**

{一段说明为什么选它——Sharpe 优势、回撤可控性、交易频次、胜率。如果亚军差距很小，要明确说出来。如果数据窗口太短或 Sharpe 边际，也要诚实点出。}

{若没有可部署的策略——例如全部带 flag 或全部 Sharpe ≤ 0——明确说明：

> 本次回测中没有任何策略达到上线门槛。建议先调整 `config.yaml` 内的参数搜索范围（例如降低 `rsi_buy`、拉长 `ma_long`），或者扩大回测窗口以覆盖更多市场状态，再考虑实盘投入。}

## 产物清单

- **资金曲线对比图**：`{COMPARISON_CHART_PATH}`
- **逐笔交易统计（CSV）**：`{TRADES_CSV_PATH}`
- **各策略 QuantStats 详细报告**：
  - `{HTML_REPORT_1}`
  - `{HTML_REPORT_2}`
  - {... 等}
- **完整运行日志**：`{RUN_LOG_PATH}`

QuantStats HTML 报告含完整的风险/收益指标（Sortino、Calmar、月度热力图、回撤明细），用浏览器打开即可深入分析。

---
*由 `backtrader-multi` skill 于 {RUN_DATE} 生成。*
