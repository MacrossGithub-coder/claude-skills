# Backtest Report — {TICKER}

**Period**: {DATA_START} → {DATA_END} ({BARS} trading days)
**Run Date**: {RUN_DATE}
**Seed**: {SEED} · **Strategies Tested**: {N_STRATEGIES}

---

## Executive Summary

{Two or three full sentences. State what was tested, which strategy ranked first by risk-adjusted return, and the headline takeaway. Write for a reader who was not in the room — full sentences, no log shorthand.}

## Strategy Comparison

| Rank | Strategy | Total Return | Annualized | Max DD | Sharpe | Trades | Win Rate |
|------|----------|-------------:|-----------:|-------:|-------:|-------:|---------:|
| 1    | {name}   | {ret}%       | {ann}%     | {dd}%  | {sharpe} | {trades} | {wr}% |
| 2    | ...      | ...          | ...        | ...    | ...    | ...    | ...      |

Sorted by Sharpe ratio (risk-adjusted return). `Sharpe = N/A` indicates the strategy did not produce enough signal to compute a meaningful ratio. Annualized return is computed from the actual data window, not the requested config period.

## Risk Flags

{If any strategy has flags, list them as bullets — strategy name in **bold**, then a plain-language explanation of what the flag means and why a reader should care. Map each technical flag to its meaning:}

- `negative_sharpe` — Risk-adjusted return is below zero. The strategy underperformed even a risk-free baseline over this window.
- `sharpe_unavailable` — Returns were too flat (or too few) for Sharpe to be defined. Treat performance numbers as low-confidence.
- `high_drawdown` — Maximum drawdown exceeded 20%. Capacity to hold through the worst loss is required.
- `no_trades` — The strategy never opened a position. Entry parameters are likely too restrictive for this ticker / window.
- `too_few_trades` — Fewer than 5 round trips. Sample size is too thin to draw conclusions.

{If no flags are present across all strategies, write: "No critical risk flags raised across the tested strategies."}

## Recommendation

**Deploy: {STRATEGY_NAME}**

{One paragraph of reasoning. Explain why this strategy beats the alternatives — Sharpe advantage, drawdown profile, trade frequency, win rate. Acknowledge the runner-up explicitly when the gap is small. If the data window is short or the Sharpe is marginal, say so.}

{If no strategy is fit for deployment — e.g. all flagged, or all Sharpe ≤ 0 — state that explicitly:

> No strategy in this run met the threshold for live deployment. Consider widening the parameter search in `config.yaml` (e.g. lower `rsi_buy`, longer `ma_long`) or extending the backtest window to capture more market regimes before committing capital.}

## Generated Artifacts

- **Equity-curve comparison**: `{COMPARISON_CHART_PATH}`
- **Trade-level statistics (CSV)**: `{TRADES_CSV_PATH}`
- **Per-strategy QuantStats reports**:
  - `{HTML_REPORT_1}`
  - `{HTML_REPORT_2}`
  - {... etc}
- **Full run log**: `{RUN_LOG_PATH}`

The QuantStats HTML reports contain the full risk and return breakdown (Sortino, Calmar, monthly heatmaps, drawdown periods). Open them in a browser for the deep dive.

---
*Generated by the `backtrader-multi` skill on {RUN_DATE}.*
